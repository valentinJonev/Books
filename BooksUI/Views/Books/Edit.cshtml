@model Book
@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/_Layout.cshtml";
}

<h2>Edit @Model.Name book</h2>

    @using (Html.BeginForm(MVC.Books.Edit(), FormMethod.Post, new { @class = "form-horizontal col-md-2", enctype = "multipart/form-data", id = "form" }))
{
    <div class="well">
        <div class="form-group">
            @Html.LabelFor(m => m.Cover)
            @Html.TextBoxFor(m => m.Cover, new { type = "file" })
        </div>
            @Html.HiddenFor(m => m.BookId)

        <div class="form-group">
            @Html.LabelFor(m => m.PublishDate)
            @Html.TextBoxFor(m => m.PublishDate, new { @class = "form-control", @Value = Model.PublishDate })
        </div>

        <div class="form-group">
            @Html.LabelFor(m => m.AuthorId)
            @Html.DropDownListFor(m => m.AuthorId, new SelectList(ViewBag.Authors, "Id", "Name"), new { @class = "form-control" })
        </div>
        <input type="submit" value="Update" onclick="document.getElementById('form').submit()" class="btn btn-default">
    </div>
}


